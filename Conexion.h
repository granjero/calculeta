#ifndef CONEXION_H
#define CONEXION_H

#include "Arduino.h" // librerias

class Conexion {

public:
   Conexion();
   void funciones();

private:
   #define CONSTANTES
   //Librerias objetos;
   void funciones_privadas();
};

#endif 

